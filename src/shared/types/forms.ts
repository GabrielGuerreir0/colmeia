export type CheckoutFormValues = {
  fullName: string;
  email: string;
  address: string;
  city: string;
  postalCode: string;
};

export type AddressForm = {
  name: string;
  email: string;
  address: string;
  city: string;
  cep: string;
};
